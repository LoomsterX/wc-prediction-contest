# Runtime dependencies for hosting (e.g. Streamlit Community Cloud).
# Mirrors pyproject.toml so non-uv hosts install everything they need.
streamlit>=1.36
pandas>=2.2
SQLAlchemy>=2.0
psycopg2-binary>=2.9
