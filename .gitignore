# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Contest database (regenerated from seed CSVs; holds live predictions locally)
data/*.db
data/*.db-journal

# Local secrets — never commit (DATABASE_URL, ADMIN_PASSWORD)
.streamlit/secrets.toml
